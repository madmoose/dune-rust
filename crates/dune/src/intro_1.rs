use crate::GameState;

impl GameState {
    pub fn play_intro_1(&mut self) {
        self.intro_part_01_init();
    }

    fn intro_part_01_init(&mut self) {
        // let hnm = self.dat_file.read("VIRGIN.HNM").unwrap();

        // let decoder = HnmDecoder::new(data, pal);
    }
}
